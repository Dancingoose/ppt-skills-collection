---
name: "mbb-decks"
description: "生成咨询级(MBB/McKinsey/Bain/BCG 风格) ghost deck 故事线。当用户要求咨询级 deck、董事会预读、高管汇报、市场进入/尽职调查/推荐方案演示时使用。采用行动标题故事线、MECE 要点、六段结构。只出故事线确认稿，不渲染 PPTX——渲染交给标准流水线（axi-front-design → html-to-pptx）。"
---

# MBB Decks — 咨询级 PPT 生成 skill

生成咨询级 ghost deck（故事线大纲）。编码了 MBB 事务所风格规则：行动标题故事线、金字塔结构、MECE 要点、引用纪律。**不渲染 PPTX**——只出故事线，渲染交给标准流水线（axi-front-design → html-to-pptx）。

## 何时使用

- 用户要求生成 MBB / McKinsey 风格 deck、咨询演示、董事会预读材料、高管汇报、市场进入 deck、尽职调查 deck、推荐方案演示。
- 用户提供了分析，需要转成给非技术高管看的幻灯片。
- 用户想要**严谨的故事线和金字塔结构**，后续渲染走标准 HTML 设计流水线。

## 工作流：先出 ghost deck，再扩展

**不要跳过 ghost-deck 步骤。** 这是最大的质量杠杆。

1. **Ghost deck。** 只为每张幻灯片生成行动标题（封面、执行摘要标题、议程项、每张内容页的行动标题、推荐标题）。从头到尾读一遍，它们必须构成一个连贯论点：情况 → 复杂化 → 解决，或 主张 → 证据 → 推荐。
2. **与用户确认。** 展示 ghost deck，问"这个故事情节成立吗？扩展前有没有要增/删/重排的？"等确认。
3. **扩展。** 填入要点、图表建议、脚注、来源。每张幻灯片在其行动标题下保持 MECE。
4. **输出确认稿，交接给标准流水线。** 产出完整的 ghost deck markdown（含行动标题、MECE 要点、图表建议、来源引用），交由 `axi-front-design` 做 HTML 设计稿 → `html-to-pptx` 导出 PPTX。咨询视觉（排版、配色、字体）在 axi-front-design 阶段按 MBB 风格约束实现。

## 六段结构

第 1-5 部分硬上限 15 页。附录不限。

| # | 部分 | 说明 |
|---|------|------|
| 1 | 封面 | 标题、副标题、作者、组织。空白主题。 |
| 2 | 执行摘要 | 一个回答"所以呢"的标题。3-5 条 MECE 要点。 |
| 3 | 议程 | 2-4 个部分。部分标题与分隔页一致。 |
| 4 | 各部分 | 每个以分隔页开头(编号+标题)。每部分 2-4 张内容页。 |
| 5 | 推荐方案 | 标题 + 行动表(负责人/行动/结果)。行动表必选。 |
| 6 | 附录 | 可选。备份细节和支持分析。同内容页格式。 |

## 行动标题规则

除封面、分隔页和附录分隔页外,每张幻灯片都有行动标题。行动标题承载含义,正文证明它。

- **一个完整句子**,10-15 词。主语+动词+主张。
- **具体。** "越南中产阶级消费到 2030 年以 7% CAGR 增长" 好于 "市场有吸引力"。
- **独立成立。** 只读行动标题的读者必须理解论证。
- **尽量有因果。** "X 驱动 Y" 好于 "X 和 Y"。
- **不要用问题和标签。** "市场容量"是标签,不是行动标题。换成发现。

写任何正文前,先按顺序读所有行动标题。如果序列不能构成连贯故事,先修故事线再扩展。

## MECE 要点

每张幻灯片最多 3-5 条要点。它们必须:
- **互斥**(Mutually Exclusive):无重叠。
- **穷尽**(Collectively Exhaustive):在行动标题下无明显缺口。
- **平行结构**:同一张幻灯片上的要点语法模式一致。
- **具体**:数字、日期、具名实体。"筛选了三家本地伙伴" 好于 "考虑了几家伙伴"。

## 视觉系统（参考指南，供 axi-front-design 实现）

本 skill 不渲染 PPTX。以下视觉规范已转译为 `mbb-consulting` 主题（完整 `:root` CSS token 见 `references/theme-tokens.md`）。执行层使用 `mbb-consulting` 主题即可自动获得正确的颜色/字体/圆角/阴影——无需手工翻译硬编码颜色。

| 元素 | 规范 |
|------|------|
| 背景 | 白色 `#FFFFFF` |
| 标题 | Georgia, 深海军蓝 `#051C2C` |
| 正文 | Calibri, 近黑 `#1A1A1A` |
| 强调色 | 电光蓝 `#2251FF`，用于规则和节编号，克制使用 |
| 网格线、分隔线 | 浅灰 `#E5E7EB` |
| 脚注、来源、页码 | Calibri 8pt, 中灰 `#949BA8` |
| 幻灯片尺寸 | 16:9, 13.333" × 7.5" |
| 边距 | 左右 0.5" |
| 页码 | 右下, 格式 `Page X / Y` |
| 页脚日期 | 从不 |
| 图标 | 从不，除非用户明确要求 |

## 图表族

选择与行动标题主张匹配的图表。

| 行动标题动词 | 图表族 |
|-------------|--------|
| "增长""下降""趋势" | 柱状或折线 |
| "由...组成""占比" | 100% 堆叠柱或饼图(饼图克制使用) |
| "高于""排名第一" | 条形(水平)按值排序 |
| "驱动""解释" | 瀑布或堆叠条 |
| "与...相关" | 散点 |
| "从 X 构建到 Y" | 瀑布 |

图表在 ghost deck 中以"图表建议"形式标注（类型 + 数据描述 + 核心信息），具体图表由 axi-front-design + ppt-visual-effects 在 HTML 中实现。

## 引用格式

- 正文内联标记:`[1]`、`[2]` 等,当具体数字或主张需要归属时。
- 幻灯片底部来源行:`Source: [text](url); [text](url)`。用 markdown 风格 `[text](url)` 语法,使每个来源在 .pptx 中渲染为可点击超链接。多个来源用分号分隔。
- 来源行上方是脚注行:对数字的解释性说明。
- 如果幻灯片没有具体数据主张,不加来源行。

## 数据来源规则(关键)

- **始终使用当前年份。** 历史数据引用最近可用版本;预测从当前年份起标 `2026E`、`2027E` 等。
- **只用权威、公认来源:** World Bank、IMF、OECD、IEA、BloombergNEF、Gartner、公司 10-K/10-Q、SEC EDGAR、美联储 等。
- **不要用:** 随机博客、AI 生成摘要、无验证引用的付费内容。
- **每个来源尽量有可点击 URL。** 找不到公共 URL 就留纯文本,不要编造 URL。

## 常见错误与修复

| 错误 | 修复 |
|------|------|
| 行动标题是标签("市场容量") | 改写成带动词和主张的句子 |
| 行动标题不连贯成故事 | 重排或重写,全部朗读一遍 |
| 每页超过 5 条要点 | 砍到 3 条或拆分幻灯片 |
| 要点重复行动标题 | 要点必须补充证据,不是重述主张 |
| 图表页无解说 | 同页加 `commentary` 面板 |
| 图表是装饰性的 | 换成能证明行动标题的图表 |
| 数据页无来源 | 加来源。每个数字主张都要归属 |
| 正文撒装饰性图标 | 移除。图标只作要点标记 |
| 页脚有日期 | 移除。house style 省略日期 |

## 红旗:遇到就停下重来

- "我先写幻灯片再想故事。" 停。先出 ghost deck。
- "行动标题太长,我缩短。" 检查仍是完整主张。如果是标签,不是正确的取舍。
- "三条要点不够,我还有很多要说。" 拆成两页或移到附录。
- "图表放一页,描述它的要点放下一页。" 停。合并。同页加 commentary 面板。
- "我加点图标让它好看。" 用 logo 作公司要点标记,抽象图标只用于概念要点。永不撒装饰性图标。

## 来源

MIT License，来自 [floflo11/mbb-decks](https://github.com/floflo11/mbb-decks)（作者 Iris Meng, 2026）。本合集版本已移除 python-pptx 渲染环节，mbb-decks 只产出 ghost deck，渲染交由标准 HTML 设计流水线。

## 交付给执行层的交接规则

ghost deck 确认后，交付给 axi-front-design 时附带以下设计约束：

1. **主题**：数据护照「主题方案」字段写 `mbb-consulting`。该主题的完整 CSS token 值见 `references/theme-tokens.md`（accent `#2251FF` / bg `#FFFFFF` / text-1 `#051C2C` / font-serif Georgia / font-sans Calibri / radius 0 / shadow none）。
2. **图标**：mbb-decks 要求"从不使用图标，除非用户明确要求"——执行层遵守此约束，禁用 lucide 图标默认。
3. **图表选择优先**：ghost deck 标注的「图表建议」（类型+数据描述）优先于 ppt-visual-effects 的自动扫描。效果模块按标注类型实现 ECharts 配置，不自行重选图表类型。
4. **页脚**：mbb-decks 禁用日期——执行层的 `.deck-footer` 不含日期字段。
5. **字号**：标题按 mbb-decks 规范（36-44pt 行动标题），正文按布局库基线（22-28px）。
6. **内容密度**：允许高密度的文本+图表同页（mbb 特征），不适用留白优先原则。
7. **CJK 字体映射**：mbb-consulting 主题的 font-display 是 Georgia，font-sans 是 Calibri——两者不含中文字形。当 deck 含中文内容时，执行层的 `--font-sans` 应为 `'Calibri','Microsoft YaHei','Noto Sans SC',sans-serif`，`--font-display` 为 `'Georgia','Microsoft YaHei','Noto Serif SC',serif`。导出 PPTX 如遇 html-to-pptx 自动种子 Noto 字体与 Calibri/Georgia 风格冲突，优先保留手动指定字体映射。

### 触发后续步骤

**ghost deck 产出后，本 skill 不自动进入渲染。** 必须显式告知用户"故事线已完成"，然后加载 `Skill("ppt-workflow")` 或让用户确认后进入标准流水线（axi-front-design 执行层的「用户提问」Phase 1/2 → 方案预览 → 展开全量 → html-to-pptx 导出）。**不做静默等待，确认交接才退场。**

