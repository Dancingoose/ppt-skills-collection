---
name: ui-ux-pro-max
description: Select precise color, typography, spacing, and component tokens for a PPT Workflow Studio design decision. Use when the task needs concrete design-system choices or verification, before the existing design decision gate.
---

# UI UX Tokens For PPT

Use the collection root named by `PPT_WORKFLOW_ROOT`; when it is unset, derive it from this installed package. Follow `ppt-workflow/SKILL.md` and read `design-assets/ui-ux-pro-max/SKILL.md` as the authority.

1. Use this bridge whenever concrete visual tokens need research or verification.
2. Translate findings into the existing passport and selected theme, preserving source and accessibility constraints.
3. Do not create a separate recipe ledger, replace creator confirmation, or bypass `check_workflow_state.py --layer decision`.
