# PPT Skill Collection — 执行纪律强化方案

> 版本：2026-08-10（第六轮审查后）
> 目的：把"依赖 agent 自觉性"的软约束，变成"有门禁、有检查、有反馈"的硬机制
> 背景：五轮审查共修复 61 项问题，但每轮都发现同一类根因——**skill 文件写了对，但 agent 实际执行时没照着走**。本方案解决的是"执行层纪律"问题，而非"文件内容"问题。

---

## 0. 为什么需要执行纪律

六轮审查暴露的问题分两类：

| 类型 | 占比 | 例子 |
|------|------|------|
| **文件内容问题**（skill 写错了） | ~40% | Step 顺序错误、颜色变量名不存在、行号引用错 |
| **执行纪律问题**（skill 写对了没照做） | ~60% | 跳过 AskUserQuestion、跳过选版式登记、展开时漂移、convert.py 忘了查 |

第二类是"写了 355 行规范，但 agent 挑着读、挑着做"——这是最大风险。**单靠把 SKILL.md 写得更长更细，边际收益递减**，因为 agent 的注意力有限。需要换机制：把关键纪律从"正文描述"提升为"强制检查点"。

---

## 1. 核心思路：三层纪律机制

```
┌─────────────────────────────────────────────────────────┐
│ 第 1 层：硬门禁（Gates）—— 不可跳过的执行闸口            │
│   在关键节点强制停下，做检查，检查不过不准往下走           │
├─────────────────────────────────────────────────────────┤
│ 第 2 层：清单校验（Checklists）—— 每层完成的强制自检      │
│   用可打勾的清单代替"请务必注意"，减少遗漏                │
├─────────────────────────────────────────────────────────┤
│ 第 3 层：反馈闭环（Feedback Loops）—— 出问题能发现能修    │
│   audit 定时做、版本同步纪律、失败复盘写入记忆             │
└─────────────────────────────────────────────────────────┘
```

每一层解决一个问题：
- 硬门禁 → 解决"跳过关键步骤"
- 清单校验 → 解决"做了一半漏细节"
- 反馈闭环 → 解决"错了不知道、知道了不改"

---

## 2. 第一层：硬门禁（6 个强制闸口）

**设计原则**：门禁要"拦得住"。放在最容易跳过的节点，检查条件要具体到"能一眼判断过没过"。

### Gate 1 · Phase 1 前置门禁
**位置**：准备层完成 → 进入设计决策层之前
**检查**：`AskUserQuestion` 已完成（受众/意图/核心主张/画布 4 项最优先）
**不过怎么办**：先问用户，问完才加载 claude-design
**现状**：ppt-workflow L128 有文字描述，但无强制检查动作

### Gate 2 · 方案预览门禁
**位置**：Step 2 用户选定 → 展开全量之前
**检查**：设计护照是否已锁定（spec_lock），且 HTML 已有方案预览（封面+1-2页）
**不过怎么办**：没有预览直接展开 = 违规，回到 Step 1
**现状**：有文字描述 + ⛔ 标记，但无清单校验

### Gate 3 · 内容核实门禁
**位置**：Step 3 选版式之前
**检查**：`content-inventory.md` 存在？内容量支撑页数？数据点有来源？
**不过怎么办**：素材不足 → 停下问用户补素材 / 做调研；数据无来源 → 不能进数据版式
**现状**：有流程描述，缺检查动作

### Gate 4 · 布局登记门禁
**位置**：Step 4 生成任何内容页之前
**检查**：每页是否有布局编号登记（A/B/C 系列）？是否匹配内容形状（P0 数据-版式匹配）？
**不过怎么办**：没登记 = 违规，回到 Step 4
**现状**：有强制步骤描述，但 HTML 注释 `<!-- LAYOUT: B6 -->` 是否真的写了靠自觉

### Gate 5 · 逐页增强门禁
**位置**：Step 6 每页展开后
**检查**：`Skill("ppt-visual-effects")` 是否已加载？每页是否扫过？
**不过怎么办**：没加载 = 违规
**现状**：L229-237 有明确描述，是"不可跳过"标注最强的步骤

### Gate 6 · 交付前置门禁
**位置**：进入交付层前
**检查**：convert.py 存在？`.config.local.toml` 已配置？
**不过怎么办**：convert.py 缺失 → 先交 HTML + 告知用户；toml 未配置 → 弹首次配置
**现状**：有 convert.py 检查，但 toml 检查是 R6-4 新加的

**落地方式**：在 ppt-workflow 的"快速参考"加一个"门禁速查表"，列出 6 个 Gate + 检查动作 + 不过的处理。每个 Gate 对应正文里的强制步骤，形成"正文详述 + 速查卡摘要 + 门禁检查动作"三层呼应。

---

## 3. 第二层：清单校验（每层一张强制自检表）

**设计原则**：清单要"能打勾"。每项是一个客观事实检查，不是主观判断。

### 准备层清单（进入第 2 层前过一遍）
```
[ ] content-inventory.md 已写入任务文件夹（D:\CLAUDEworkspace\work\<任务名>\ 或已告知用户回退路径风险）
[ ] 源素材类型已判定（docx/pdf/pptx/图片/口述）并用了对应 skill
[ ] 口述主题 → WebSearch 已做，数据点有来源 URL
[ ] 页数/章节预判已写入
[ ] convert.py 存在性已检查（提前门禁）
```

### 设计决策层清单（方案预览前过一遍）
```
[ ] Phase 1 完成（受众/意图/核心主张/画布）
[ ] 按场景选了 skill（claude-design / ui-ux-pro-max / mbb-decks / 跳过）
[ ] 方向名称用了 10 种设计语言的精确名称（映射表可匹配）
[ ] Phase 2 完成（页数/主题方案/风格版本数等）
[ ] 反模板审查已做（frontend-design，一次即可）
[ ] 设计护照 4 个关键字段已填：配色 hex / 字体 / 风格方向 / 主题方案
```

### 执行层清单（Step 4-7 自检）
```
[ ] quick-reference-card 已读
[ ] 每页已登记布局编号（HTML 注释 LAYOUT: X）
[ ] 数据-版式 P0 匹配（有数据用 B6/B7/B20/B21，无数据禁 B6/B7）
[ ] 事实风(B) 直角无阴影，叙事风(A)/Bento(C) 用圆角
[ ] --font-display 含 Inter/Roboto 已替换为 Noto Sans SC / Microsoft YaHei
[ ] 8页+ deck 已画节奏表（先画表再动手）
[ ] 无 3 页连续同主题（light/dark 交替）
[ ] 展开时未漂移（配色/字体/风格与锁定护照一致）
[ ] ppt-visual-effects 已加载，每页扫过（含判断"不需要"的页）
[ ] Canvas/WebGL pointer-events: none
[ ] 增强代码颜色走 CSS 变量，无硬编码 hex
```

### 交付层清单（转换前过一遍）
```
[ ] convert.py 存在
[ ] .config.local.toml 已配置（无则弹首次配置）
[ ] 用户已确认交付形态（HTML / PPTX / 纯PPTX）
[ ] Canvas/WebGL 增强已告知会降级为静态截图
[ ] 转换后视觉 audit 已跑（triage 模式）
```

**落地方式**：
1. 在 ppt-workflow 正文每个 Step 后追加对应清单（`- [ ]` 格式）
2. 在 quick-reference-card.md 末尾加"四层清单速查"（压缩版，每层 4-6 项最关键的）
3. agent 完成每层时，把清单输出在回复里（让用户能看到"这层查了哪些"）——**透明化强制**，用户监督比自己监督有效

---

## 4. 第三层：反馈闭环（3 条持续机制）

### 机制 A · 版本同步纪律（防"修了等于没修"）

这是 R5/R6 反复踩的坑：**合集修了，installed 版没同步 → 白修。**

规则：
1. **任何对合集 SKILL.md 的修改，必须同步 installed 版**（save_skill overwrite:true）
2. 同步后必须验证：`diff` 从 `# 标题` 起正文 IDENTICAL
3. 每次 session 开始做 PPT 任务前，先验证一次同步状态（~30 秒）

验证脚本（写入合集，供每次检查用）：
```bash
# 检查 7 个核心 skill 是否与合集同步
for skill in ppt-workflow axi-front-design ppt-visual-effects html-to-pptx claude-design mbb-decks; do
  inst=$(grep -n "^# " ~/.claude/skills/$skill/SKILL.md | head -1 | cut -d: -f1)
  coll=$(grep -n "^# " /d/CLAUDEworkspace/work/ppt-skills-collection/$skill/SKILL.md 2>/dev/null | head -1 | cut -d: -f1)
  # 若 collection 路径不同则按实际路径
  diff <(tail -n +${inst} ~/.claude/skills/$skill/SKILL.md) <(tail -n +${coll} /path/to/collection/$skill/SKILL.md) >/dev/null 2>&1 \
    && echo "✅ $skill" || echo "❌ $skill 不同步"
done
```

### 机制 B · 定时审查节奏（防"问题积累成山"）

| 频率 | 动作 | 产出 |
|------|------|------|
| 每次做 PPT 后 | 快速复盘：哪层跳过/哪步含糊/哪里漂移 | 一句话记录 |
| 每 5 次 PPT 任务 | 一次全流程模拟（像 R3-R6 那样 walkthrough） | 审查报告 |
| 每轮审查后 | 把新问题修复 + 同步 installed 版 + 更新记忆 | 无遗留 |

**关键**：审查不只是"找问题"，更是"验证上次修复是否真落地"（回归验证）。R5 就干了这事（验证前四轮 58 项修复无复发）。

### 机制 C · 失败复盘写记忆

遇到"执行层跳步/漂移/漏检查"时：
1. 把具体场景写进记忆（`feedback` 类型：规则 + Why + How to apply）
2. 在下一次做 PPT 时主动检查该场景
3. 如果连续 2 次犯同一类错 → 升级为硬门禁（从第 2 层提升到第 1 层）

> 举例：如果多次发现"展开时偷偷改了字体" → 把 spec_lock 从文字描述升级为"展开前必须输出锁定护照 + 展开后 diff 对比"的硬检查。

---

## 5. 落地优先级

| 优先级 | 动作 | 工作量 | 收益 |
|--------|------|--------|------|
| **P0** | 6 个硬门禁写进 ppt-workflow + quick-reference-card | 小（改 2 个文件） | 拦掉 80% 的跳步问题 |
| **P0** | 版本同步纪律落地（每次改 skill 必同步+验证） | 小（写进记忆） | 防"白修" |
| **P1** | 四层清单校验写进 ppt-workflow 各 Step | 中 | 减少细节遗漏 |
| **P1** | agent 完成每层时输出清单（透明化） | 无（改习惯） | 用户可监督 |
| **P2** | 每 5 次任务一次全流程模拟 | 中 | 持续发现新问题 |
| **P2** | 失败复盘写记忆机制 | 无 | 越用越稳 |

---

## 6. 具体执行方案（给 agent 的"操作规程"）

当收到"做个 PPT"请求时，**在回复里输出以下进度标记**，让每一步都可见可查：

```
🏁 准备层
   [Gate1] 素材判定: 口述 → WebSearch 已做，数据 3 点有来源
   [Gate2] content-inventory.md 已写入 D:\CLAUDEworkspace\work\AI趋势分析\
   [Gate3] convert.py 存在 ✅
🏁 设计决策层
   [Gate1] Phase 1 完成（受众=管理层/意图=说服/主张=AI加速产业转型/画布=16:9）
   [Gate2] claude-design 出 3 方向: 瑞士编辑式/包豪斯/动效诗学
   [Gate3] Phase 2 完成（10页/主题=swiss-grid/3版）
   [Gate4] 反模板审查通过（无 AI 俗套选型）
🏁 执行层
   [Gate1] 方案预览已出 3 版，用户选定 swiss-grid
   [Gate2] 护照已锁定
   [Gate3] content-inventory 核实: 10页可行，数据有来源
   [Gate4] 布局登记: B1/B6/B2/B11/B22/...（每页已注释 LAYOUT: X）
   [Gate5] 节奏表已画: 8页 light/dark 交替
   [Gate6] 逐页增强已扫（3页ECharts/1页Shadertoy/4页跳过）
🏁 交付层
   [Gate1] convert.py ✅ / toml 已配置
   [Gate2] 转换完成，视觉 audit 通过
```

这套"进度标记"就是执行纪律的可视化——**agent 每一步都有检查，用户每一步都能看见**。

---

## 7. 与现有文档的关系

本方案不替代现有 SKILL.md，而是**补充三层机制**：
- ppt-workflow/SKILL.md → 补充 6 个硬门禁 + 四层清单（正文详述）
- quick-reference-card.md → 补充"门禁速查表" + "四层清单速查"（精简版）
- MEMORY.md → 补充"版本同步纪律" + "执行纪律强化"两条记忆
- 本文件 → 作为合集内"执行纪律规范"参考文档

最终状态：**正文管"怎么做好"，门禁管"必须做到"，清单管"有没有做全"，记忆管"错了记得改"**。

---

## 8. 成功指标

| 指标 | 现状（R6） | 目标 |
|------|-----------|------|
| 每轮审查发现的"执行纪律类"问题 | ~60% | < 20% |
| 每次 PPT 任务的完整流程执行率 | 依赖自觉 | ≥ 90%（靠门禁+清单） |
| 版本同步遗漏次数 | R5/R6 各 1 次（claude-design/mbb-decks） | 0 |
| 用户需要重复纠正的同类错误 | 存在 | 0（复盘写记忆） |
