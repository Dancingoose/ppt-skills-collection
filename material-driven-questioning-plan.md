# 材料驱动提问（Material-Driven Questioning）设计方案

> 版本：v1 草案（2026-08-10）
> 目标：把 Phase 1 沟通契约从"固定模板选项"升级为"材料驱动选择题"——每个问题的选项由 agent 读完 content-inventory.md 后，从材料内容里提炼生成。
> 决策已确认：扩展到 12 项 | 核心主张用"提炼候选 + Other 兜底" | 先出方案文档再改

---

## 1. 为什么需要这个改动

现有 Phase 1 的选项是**固定骨架**：

| 维度 | 现有选项 | 问题 |
|------|---------|------|
| 受众 | `内部团队` · `管理层` · `外部客户/投资人` · `公开分享` | 永远这 4 个，和"AI 行业趋势"或"公司年度财报"没区别 |
| 核心主张 | 用户自由输入 | 是唯一开放项，但用户常常"说不太清"，浪费第一轮 |
| 画布格式 | `16:9` · `4:3` · `小红书 3:4` · `故事 9:16` | 不告诉用户"你的材料更适合哪种" |

问题是：**这些选项没有和材料建立联系**。agent 明明读完了 content-inventory.md，知道材料里有什么数据、什么故事、什么语气，但问出来的问题却和材料无关。

**改动的核心**：选项不是"预置的"，而是 agent 每轮从材料里**现场生成**的。生成依据来自 content-inventory.md 的内容特征（数据点、章节结构、语气、图表类型）。

---

## 2. 材料特征 → 提问选项的推导引擎

设计一个"材料特征扫描器"，agent 读 content-inventory.md 时并行提取以下 6 类特征，每类特征喂给对应维度的选项生成：

| 材料特征 | 提取来源 | 喂给哪些维度 |
|---------|---------|-------------|
| **数据形态** | 数据点清单：是否有市场规模/CAGR/占比/对比数字 | 受众、沟通意图、期望结果、画布 |
| **章节骨架** | 页数/章节预判：有几个章节、什么主题 | 故事线、核心主张、场景 |
| **语气/立场** | 源内容全文的措辞：客观分析 or 带倾向 | 沟通意图、期望结果 |
| **图表暗示** | 数据点类型：趋势→折线、占比→饼图、对比→柱状 | 画布、故事线 |
| **受众线索** | 素材来源（研报/内部文档/公开演讲）、提及的人/公司 | 受众、场景、交付用途 |
| **内容密度** | 材料体量、每个章节的信息量 | 页数（Phase 2）、信息密度（新增项） |

**操作规则**：每个选项必须能在 content-inventory.md 里找到**出处**（一个数字、一句话、一个章节）。找不到出处的选项不允许出现。这保证了"材料驱动"不是空话。

---

## 3. 扩展后的 12 项 Phase 1

在现有 9 项基础上加 3 项（用 ★ 标记新增）。每项都给出"选项从材料哪里来"的推导说明。

### 第一优先组（最重要的 4 项，先问）

**Q1 · 受众** `受众`
> 选项从材料的"受众线索"推导：素材是研报 → 投资/研究；内部材料 → 内部团队；用户提及"要跟老板汇报" → 管理层。找不到明确线索时，从数据深度反推（数据很深 → 专业受众；很浅 → 泛受众）。

示例（AI 行业材料）：
- `投资机构 / 决策层`（材料有市场规模+CAGR，适合投资视角）
- `企业高管 / 技术决策者`（材料含落地案例和成本）
- `行业从业者 / 开发者`（材料偏技术演进）
- `学术 / 研究圈层`（材料含研究方法）

**Q2 · 沟通意图** `沟通意图`
> 从材料"语气/立场"推导：材料在论证"AI 值得投入" → 说服；材料是客观梳理 → 汇报/教学；材料有强烈个人观点 → 分享。若材料同时含多种语气，优先标推荐项。

示例：
- `说服——推动 AI 转型/投资决策`（材料有 ROI 和机会成本论证）
- `汇报——呈现行业趋势研究结论`（材料是客观数据梳理）
- `教学——解释 AI 行业现状与关键概念`（材料有概念解释和入门内容）
- `分享——表达对 AI 未来方向的判断`（材料有观点性段落）

**Q3 · 核心主张** `核心主张`
> **这是本次改动的重点。** 不再是空白输入框。agent 从材料的"核心信息"（content-inventory.md 已有"一句话总结"字段）和最强数据点中，提炼 2-3 个候选主张作为选项，用户直接选，或点 Other 自己写。

示例：
- `AI 正从技术试验走向规模化落地，2026 是关键窗口期`
- `中国 AI 市场增速领跑全球，但核心技术差距仍需追赶`
- `AI 成本快速下降正在重塑行业竞争格局`

**Q4 · 画布格式** `画布`
> 从"数据形态"和"图表暗示"推导：纯数据报告 → 16:9 演示（推荐）；含大量图文可传播内容 → 小红书 3:4；学术报告 → 4:3。**每个候选选项都要标注"为什么你的材料适合它"**。

示例：
- `16:9 PPT（推荐）`（材料是数据型报告，横向 Z 型阅读）
- `4:3 投影`（若材料含学术/传统汇报特征）
- `小红书 3:4`（若材料含可传播的图文/知识点）

### 第二优先组（次重要，第二批问）

**Q5 · 语言** `语言`
> 从材料语言判断：材料是中文 → 中文（推荐）；英文 → 英文；中英混合 → 中英混排。**选项直接来自材料的实际语言，不空问。**

**Q6 · 期望结果** `期望结果`
> 从"沟通意图"联动推导：意图是说服 → 做决策；汇报 → 记住结论；教学 → 掌握方法。选项与 Q2 意图保持一致。

**Q7 · 场景/用途** `场景`
> 从"受众线索 + 交付用途"推导：受众是管理层 → 会议室投影；外部客户 → 线上会议/公开演讲。若有不确定，选项给全。

### 第三优先组（最后问，可选）

**Q8 · 交付用途** `交付用途`
> 从"受众 + 场景"联动推导：现场演讲 → 读屏；自看 → 自读文档；外发 → 别人看。默认现场演讲。

**Q9 · 故事线** `故事线`
> **从"章节骨架"推导**：材料章节是"现状→问题→方案" → 三段式；"数据先行→论证→结论" → 数据式；"开场→推进→收尾" → 经典式。选项直接对应材料章节的自然顺序。

示例（AI 材料，章节为"市场现状/技术突破/落地案例/未来趋势"）：
- `数据先行 → 论证 → 结论`（材料前两章是数据，后两章是推断）
- `开场立题 → 分点推进 → 收尾号召`（材料四章并列推进）
- `你来定`

**Q10 · 内容侧重点** `侧重点` ★（新增）
> 从"章节骨架 + 数据形态"推导：材料哪些章节数据最全 → 数据为重点；哪些章节是观点 → 观点为重点。**这决定后续选版式和增强的优先级**（数据多的页用 B6/B7，观点页用 A 系列）。

示例（AI 材料）：
- `市场数据为主`（市场规模/增长率章节数据最全）
- `技术演进为主`（技术章节是核心贡献）
- `落地案例为主`（案例章节最生动）
- `综合均衡`

**Q11 · 信息密度** `信息密度` ★（新增）
> 从"内容密度"推导：材料体量大、每章信息多 → 高密度（每页信息多，文字密）；材料精简 → 中/低密度。**影响字号强度和版式选择**（高密度用小字号多栏，低密度用大字号留白）。

示例：
- `高密度——每页承载多要点，专业受众`（材料每章 3-5 个要点）
- `中密度——每页 2-3 要点，平衡`（推荐）
- `低密度——每页 1 个大观点，留白多`（材料观点精简）

**Q12 · 参考风格** `参考风格` ★（新增）
> **从材料里找"对标物"**：材料是"极简风"→ 对应 `minimal-white`；"科技感"→ `aurora`/`glassmorphism`；"咨询风"→ `mbb-consulting`。选项直接映射到主题库，用户选风格时已经对应到具体主题。

示例（AI 材料）：
- `科技感（深色 + 发光强调）`→ aurora / cyberpunk-neon
- `极简商务（白底 + 克制强调）`→ minimal-white / corporate-clean
- `数据感（网格 + 大数字）`→ swiss-grid / editorial-serif
- `你来定`

---

## 4. 分批规则（12 项怎么问）

单次 `AskUserQuestion` 最多 4 问。12 项分 3 批：

| 批次 | 问题 | 为什么这批 |
|------|------|-----------|
| **第一批** | Q1 受众 · Q2 意图 · Q3 主张 · Q4 画布 | 4 个最关键维度，决定方向 |
| **第二批** | Q5 语言 · Q6 期望结果 · Q7 场景 · Q8 交付 | 4 个次关键维度，补充上下文 |
| **第三批** | Q9 故事线 · Q10 侧重点 · Q11 信息密度 · Q12 参考风格 | 4 个执行相关维度，指导版式 |

**跳过规则**：
- 用户已明说的维度，跳过不问
- 材料特征能明确确定的维度（如语言），可只给推荐项 + 1-2 个备选，不铺满 4 项
- 口述主题无源文件的场景（没有 content-inventory.md），**回退到现有固定骨架**——材料驱动的前提是有材料可读

---

## 5. 落地到 skill 的改动点

### 改动文件 1：`axi-front-design/SKILL.md`
替换现有 Phase 1 表格（9 行）为 12 行版，每行新增一列「选项来源」（材料特征推导说明）。

### 改动文件 2：`ppt-workflow/SKILL.md`
在 Phase 1 相关段落补充"材料驱动选项"原则 + 回退规则（无材料时用固定骨架）。

### 改动文件 3：`references/quick-reference-card.md`
Phase 1 速查部分更新为 12 项 + 3 批次分组。

### 改动文件 4（新增）：`references/material-driven-questioning.md`
完整的"材料特征 → 选项"推导引擎 + 12 项明细 + 示例（本文档的精华版）。

### 同步
改完合集后，save_skill overwrite:true 同步 axi-front-design 和 ppt-workflow 到 installed 版。

---

## 6. 边界与风险

| 风险 | 缓解 |
|------|------|
| 无材料场景（纯口述）没有可提炼内容 | 回退到现有固定骨架，材料驱动只在有 content-inventory.md 时启用 |
| 选项"硬造"：材料里没有明确线索，强行编选项 | 规则明确"找不到出处的选项不允许出现"；线索不足时该维度回退到通用骨架 |
| 12 项太多，用户烦 | 跳过规则 + 分批（每批 ≤4）+ 材料能确定的维度只给推荐项 |
| 核心主张提炼不准 | 用 content-inventory.md 的"核心信息"字段（已有一句话总结）+ 最强数据点双重锚定；用户可 Other 自由输入 |
| 与 Phase 2 的主题方案选项冲突 | 材料驱动只改 Phase 1 的"方向类"问题；Phase 2 的"设计类"问题（页数/主题/风格数）保持现有逻辑，但 Q12 参考风格的结果会预填 Phase 2 主题选项 |

---

## 7. 验收标准

- [ ] 有材料时，Phase 1 的每个问题选项都能在 content-inventory.md 找到出处
- [ ] 无材料时，自动回退到固定骨架（不报错、不强问）
- [ ] 12 项分 3 批，每批 ≤ 4 问
- [ ] 核心主张始终有 2-3 个材料提炼的候选 + Other 兜底
- [ ] Q12 参考风格的选项能映射到具体主题名
- [ ] 改动后 installed 版与合集版正文 IDENTICAL
